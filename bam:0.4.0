FROM debian:oldstable-slim AS build
RUN apt update
RUN apt install -y \
	build-essential \
	git

RUN git clone \
	--no-checkout \
	https://github.com/matricks/bam

WORKDIR bam
RUN git checkout v0.4.0
RUN ./make_unix.sh

FROM debian:oldstable-slim
COPY --from=build /bam /bam
